services:
  - type: web
    name: entity-json-server
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.20.0
      - key: PORT
        generateValue: true
      - key: NODE_ENV
        value: production
